<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Company</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .form-container {
            max-width: 600px;
            margin: auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
        }
        label {
            font-weight: bold;
            display: block;
            margin: 10px 0 5px;
        }
        input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .button {
            width: 100%;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
            text-align: center;
            font-size: 16px;
            border-radius: 4px;
        }
        .button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

<div class="form-container">
    <h1>Edit Company</h1>
    <form action="/companies/edit/<%= company._id %>" method="POST">
        <label for="item">Item:</label>
        <input type="text" id="item" name="item" value="<%= company.item %>" required>

        <label for="company">Company:</label>
        <input type="text" id="company" name="company" value="<%= company.company %>" required>

        <label for="dealer">Dealer:</label>
        <input type="text" id="dealer" name="dealer" value="<%= company.dealer %>" required>

        <h3>Salesmen</h3>
        <div id="salesmanContainer">
            <% company.salesMan.forEach((salesman, index) => { %>
                <div class="salesman">
                    <label for="salesManName_<%= index %>">Salesman Name:</label>
                    <input type="text" id="salesManName_<%= index %>" name="salesMan[<%= index %>][name]" value="<%= salesman.name %>" required>
                    
                    <label for="salesManContact_<%= index %>">Contact Number:</label>
                    <input type="text" id="salesManContact_<%= index %>" name="salesMan[<%= index %>][contactNumber]" value="<%= salesman.contactNumber %>" required>
                </div>
            <% }) %>
        </div>

        <button type="submit" class="button">Save Changes</button>
    </form>
</div>

</body>
</html>
